* This page describes the command sets used to interact with Arduino. 

= Instruction Map =

This section describes the command mapping used to alter the state of an arduino connected via the andruino web service. Each instruction in the protocol will consist to 3 bytes. 

== References / Resources ==

 * [http://arduino.cc/en/Reference/HomePage]
 * [http://pyserial.sourceforge.net/]


== Serial Frame ==

 * Faming Byte   0xFF
 * Command Bytes(2 Read / Wite ) 
  # Read 0x72 ("r") 
  # Write 0x77("w")
 * AVR register Byte 
 * Register Value - Note there are only 2 registers in 


== AVR Register Bits ==

||Hex||H||L||Port||Register||
||0x11||0001||0001||A||DDR|| 
||0x12||0001||0010||A||
==Examples==







|| *Command* || *Description* || *Full Command Sequence* ||
|| B || Control IO on the Arduino B register. || B1L - Set Pin 9 to low ||